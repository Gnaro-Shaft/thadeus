# Modèle jouet — pour les tests et le débogage de la chaîne, jamais pour juger
# de la qualité. Doit s'entraîner en quelques secondes.
extends = "model/small.toml"

vocab_size = 512
d_model = 128
n_layers = 2
max_seq_len = 128

[attention]
name = "gqa"
n_heads = 2
n_kv_heads = 1
head_dim = 64
